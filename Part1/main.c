#include "int.h"

int main (void){

    relation* relR = createRelation();
    //sleep(1);                 
    relation* relS = createRelation();
    
    






    relationDelete(relR);
    relationDelete(relS);
}